# 开发环境通过 Vite 的 /api 代理访问后端，浏览器无需处理跨域。
VITE_API_BASE_URL=/api

# 本地 Nginx 使用 80 端口提供部署后的网站。
VITE_DEPLOY_DOMAIN=http://localhost

# Vite 开发服务器将 /api 请求转发到 Spring Boot。
API_PROXY_TARGET=http://localhost:8101
